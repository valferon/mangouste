{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window and every window opened beside it",
  "windows": ["main", "window-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:window:allow-set-fullscreen",
    "core:webview:allow-set-webview-zoom",
    "dialog:default",
    "opener:default"
  ]
}
